## 2024-05-23 - Accessibility improvements for icon-only buttons
**Learning:** Icon-only buttons using SVG or HTML entities lack context for screen readers and require explicit `aria-label` attributes to ensure accessibility.
**Action:** Always verify icon-only buttons in HTML and add descriptive `aria-label` attributes.
